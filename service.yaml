apiVersion: v1
kind: Service
metadata:
  name: frost-app
  labels:
    app: frost-app
spec:
  type: LoadBalancer
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
    name: http
  selector:
    app: frost-app
  sessionAffinity: None